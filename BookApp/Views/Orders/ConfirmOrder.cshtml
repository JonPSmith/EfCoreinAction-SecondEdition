@model ServiceLayer.OrderServices.OrderListDto
@{
    ViewData["Title"] = "Confirm Order";
}

<div class="container">
    <h3>Your order is confirmed</h3>
    @Html.Partial("OneOrderPartial", @Model)

    <p><strong>NOTE:</strong> This is a demo, so your order will NOT be delivered</p>

    @Html.ActionLink("See all your orders", "Index", new {}, new { @class = "btn btn-primary" })
</div>